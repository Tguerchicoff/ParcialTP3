package com.ort.edu.ar.parcialtp3.Activities

import androidx.appcompat.app.AppCompatActivity
import android.os.Bundle
import com.ort.edu.ar.parcialtp3.R

class WelcomeScreenActivity : AppCompatActivity() {
    override fun onCreate(savedInstanceState: Bundle?) {
        super.onCreate(savedInstanceState)
        setContentView(R.layout.fragment_welcome_screen)

        // No es necesario gestionar fragmentos manualmente

        // Asegúrate de que el fragmento se está mostrando automáticamente en el layout

    }
}
